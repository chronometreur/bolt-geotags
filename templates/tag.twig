<meta name="geo.region" content="{{ countrycode }}-{{ regionalcode }}" />
<meta name="geo.placename" content="{{ ort }}" />
<meta name="geo.position" content="{{ latitude }};{{ longitude }}" />
<meta name="ICBM" content="{{ latitude }}, {{ longitude }}" />